<Window x:Class="DynamicWallpaperStudio.DependencySetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="动态壁纸工作室 · 首次准备"
        Width="520" Height="230"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True">
  <Grid Background="{StaticResource AppBackground}" Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Text="正在准备视频播放组件" FontSize="20" FontWeight="SemiBold" />
    <TextBlock x:Name="StatusText" Grid.Row="1" Margin="0,14,0,10"
               Foreground="{StaticResource SecondaryText}" Text="正在连接下载服务器…" />
    <ProgressBar x:Name="DownloadProgress" Grid.Row="2" Height="12" Minimum="0" Maximum="100" />
    <DockPanel Grid.Row="3" Margin="0,18,0,0" LastChildFill="True">
      <Button x:Name="CancelButton" DockPanel.Dock="Right" Width="86" Content="取消" Click="CancelClick" />
      <TextBlock VerticalAlignment="Center" Foreground="{StaticResource SecondaryText}"
                 Text="下载文件会先经过 SHA-256 校验" />
    </DockPanel>
  </Grid>
</Window>
